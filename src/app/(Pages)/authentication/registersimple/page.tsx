import RegisterSimpleContainer from "@/Components/Other/Authentication/RegisterSimple/RegisterSimple";
import React from "react";

const RegisterSimple = () => {
  return <RegisterSimpleContainer />;
};

export default RegisterSimple;
