import LoginWithBackGroundImageContainer from "@/Components/Other/Authentication/LoginWithBackGroundImage/LoginWithBackGroundImage";

const page = () => {
  return <LoginWithBackGroundImageContainer />;
};

export default page;
